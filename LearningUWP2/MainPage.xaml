<Page
    x:Class="LearningUWP2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LearningUWP2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="TruckDetailTemplate">
                <StackPanel x:Name="TruckPanel">
                    <TextBlock Text="{Binding Id}"/>
                    <TextBlock Text="{Binding Name}"/>
                    <TextBlock Text="{Binding Style}"/>
                </StackPanel>
            </DataTemplate>
        </Grid.Resources>
        <!--<ContentControl ContentTemplate="{StaticResource TruckDetailTemplate}"/>-->
        <!--<ItemsControl ItemsSource="{Binding}" ItemTemplate="{StaticResource TruckDetailTemplate}"/>-->
        <!--<ItemsControl ItemsSource="{Binding Trucks}" ItemTemplate="{StaticResource TruckDetailTemplate}"/>-->
        <ListBox SelectedItem="{Binding SelectedTruck, Mode=TwoWay}" ItemsSource="{Binding Trucks}" ItemTemplate="{StaticResource TruckDetailTemplate}" />


    </Grid>
</Page>
